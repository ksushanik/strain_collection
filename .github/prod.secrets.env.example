# Production deployment secrets template
# Create repository-level secrets in GitHub with these names and values.
# Do NOT commit real secrets. This file is an example with comments.

# Docker Hub credentials
DOCKERHUB_USERNAME=your_dockerhub_username
DOCKERHUB_TOKEN=your_dockerhub_access_token

# SSH private key for production server
# Use an OpenSSH PEM private key. Ensure LF line endings.
# If you copied on Windows, convert CRLF to LF (the workflow also normalizes).
PROD_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----\n...

# SSH connection parameters
PROD_SSH_HOST=culturedb.elcity.ru
PROD_SSH_USER=deploy
PROD_SSH_PORT=22

# Known hosts entry for the production server
# Obtain using: ssh-keyscan -H culturedb.elcity.ru
# Multiple lines are allowed; paste as a single string in the secret.
PROD_SSH_KNOWN_HOSTS=culturedb.elcity.ru ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA...

# Remote directory on the production server
# Example: the working directory used by docker compose
PROD_REMOTE_DIR=~/strain_ultra_minimal

# Health check URL (optional)
# If set, the workflow will verify successful API response after deploy
PROD_HEALTHCHECK_URL=https://culturedb.elcity.ru/api/health/