# Django настройки
DEBUG=True
SECRET_KEY=your-dev-secret-key-here-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# База данных (PostgreSQL в Docker) - правильные имена переменных для Django
POSTGRES_DB=strain_collection
POSTGRES_USER=strain_user
POSTGRES_PASSWORD=strain_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# CORS для разработки
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Логирование
LOG_LEVEL=DEBUG
